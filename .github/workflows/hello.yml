name: C/C++ CI

on:
  push:
    branches: [ $default-branch ]

jobs:
  hello:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Say hello
      run: echo "Hello world"